# ==========================================
# SEPULKI FORGE UI - ENVIRONMENT VARIABLES
# ==========================================
# This file contains all environment variables needed for the Sepulki Forge frontend application.
# Copy this file to .env.local for development or configure in your deployment platform.

# ==========================================
# NODE ENVIRONMENT
# ==========================================
# Environment: development, production, or test
NODE_ENV=development

# ==========================================
# NEXT.JS CONFIGURATION
# ==========================================
# Next.js host binding (use 0.0.0.0 for Docker, localhost for local dev)
# HOSTNAME=0.0.0.0
# PORT=3000

# ==========================================
# API ENDPOINTS
# ==========================================
# GraphQL API endpoint (Hammer Orchestrator)
# Development: http://localhost:4000/graphql
# Production: https://your-domain.com/graphql
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4000/graphql

# REST API endpoint (for file uploads and other REST endpoints)
# Development: http://localhost:4000
# Production: https://your-domain.com
NEXT_PUBLIC_API_URL=http://localhost:4000

# WebSocket endpoint for GraphQL subscriptions (optional)
# Development: ws://localhost:4000/graphql
# Production: wss://your-domain.com/graphql
# NEXT_PUBLIC_GRAPHQL_WS_URL=ws://localhost:4000/graphql

# ==========================================
# AUTHENTICATION (NextAuth.js)
# ==========================================
# NextAuth.js base URL
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL=http://localhost:3000

# NextAuth.js secret for JWT signing (REQUIRED IN PRODUCTION)
# Generate with: openssl rand -base64 32
# ⚠️  WARNING: Never commit real secrets to version control!
NEXTAUTH_SECRET=your-nextauth-secret-change-in-production

# ==========================================
# LOCAL DEVELOPMENT OAUTH (Optional)
# ==========================================
# Local OAuth provider (Hydra) - for development only
# Leave commented for mock authentication
# LOCAL_OAUTH_ISSUER=http://localhost:4444
# LOCAL_OAUTH_CLIENT_ID=sepulki-forge
# LOCAL_OAUTH_CLIENT_SECRET=sepulki-forge-secret

# ==========================================
# GITHUB OAUTH (Production)
# ==========================================
# GitHub OAuth Application credentials
# Get from: https://github.com/settings/developers
# Callback URL: https://your-domain.com/api/auth/callback/github
# GITHUB_CLIENT_ID=your-github-oauth-client-id
# GITHUB_CLIENT_SECRET=your-github-oauth-client-secret

# ==========================================
# GOOGLE OAUTH (Production)
# ==========================================
# Google OAuth Application credentials
# Get from: https://console.cloud.google.com/apis/credentials
# Callback URL: https://your-domain.com/api/auth/callback/google
# GOOGLE_CLIENT_ID=your-google-oauth-client-id
# GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# ==========================================
# EMAIL SERVICE (Development)
# ==========================================
# Local email service (MailHog) - development only
# EMAIL_SERVER=smtp://localhost:1025
# EMAIL_FROM=noreply@sepulki.local

# Production email service (SendGrid, AWS SES, etc.)
# EMAIL_SERVER=smtp://smtp.sendgrid.net:587
# EMAIL_FROM=noreply@sepulki.com
# SENDGRID_API_KEY=your-sendgrid-api-key

# ==========================================
# ISAAC SIM INTEGRATION
# ==========================================
# Isaac Sim WebSocket endpoint
# Development: Local or AWS EC2 instance
# Production: Load-balanced Isaac Sim cluster
NEXT_PUBLIC_ISAAC_SIM_IP=18.234.83.45
NEXT_PUBLIC_ISAAC_SIM_PORT=8211

# AWS Isaac Sim endpoint (if different from above)
# NEXT_PUBLIC_AWS_ISAAC_SIM_IP=18.234.83.45

# Isaac Sim WebSocket URL (optional, constructed from IP/port if not provided)
# NEXT_PUBLIC_ISAAC_SIM_WS_URL=ws://18.234.83.45:8211

# ==========================================
# VIDEO STREAMING
# ==========================================
# Video stream proxy service endpoint
# Development: http://localhost:8889
# Production: https://video-proxy.your-domain.com
NEXT_PUBLIC_VIDEO_PROXY_URL=http://localhost:8889

# ==========================================
# FILE STORAGE (MinIO/S3)
# ==========================================
# MinIO endpoint for file uploads (blueprints, routes, etc.)
# Development: http://localhost:9000
# Production: https://s3.your-domain.com or https://your-bucket.s3.amazonaws.com
NEXT_PUBLIC_MINIO_ENDPOINT=http://localhost:9000

# MinIO/S3 bucket name
# NEXT_PUBLIC_MINIO_BUCKET=sepulki-files

# MinIO/S3 public URL (if different from endpoint)
# NEXT_PUBLIC_MINIO_PUBLIC_URL=https://cdn.your-domain.com

# ==========================================
# OPENAI API (AI Features)
# ==========================================
# OpenAI API key for AI-powered features (optional)
# Get from: https://platform.openai.com/api-keys
# ⚠️  WARNING: This is a server-side only secret. Never expose in NEXT_PUBLIC_* vars!
OPENAI_API_KEY=your-openai-api-key

# ==========================================
# ERROR TRACKING (Sentry)
# ==========================================
# Sentry DSN for error tracking (optional)
# Get from: https://sentry.io/settings/projects/
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project-id

# Sentry environment
# NEXT_PUBLIC_SENTRY_ENVIRONMENT=development

# Sentry traces sample rate (0.0 to 1.0)
# NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.1

# ==========================================
# ANALYTICS (Optional)
# ==========================================
# Google Analytics tracking ID
# NEXT_PUBLIC_GA_TRACKING_ID=UA-XXXXXXXXX-X

# PostHog analytics (optional)
# NEXT_PUBLIC_POSTHOG_KEY=your-posthog-key
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ==========================================
# FEATURE FLAGS
# ==========================================
# Enable experimental features
# NEXT_PUBLIC_ENABLE_EXPERIMENTAL_FEATURES=false

# Enable debug mode (shows extra logging)
# NEXT_PUBLIC_DEBUG_MODE=false

# ==========================================
# DEPLOYMENT PLATFORM VARIABLES
# ==========================================
# These are automatically set by deployment platforms, but can be overridden

# Vercel deployment URL (auto-set by Vercel)
# VERCEL_URL=auto-detected-by-vercel

# Railway deployment URL (auto-set by Railway)
# RAILWAY_STATIC_URL=auto-detected-by-railway

# Render deployment URL (auto-set by Render)
# RENDER_EXTERNAL_URL=auto-detected-by-render

# ==========================================
# SECURITY SETTINGS
# ==========================================
# Content Security Policy (CSP) settings
# Uncomment and adjust for production
# NEXT_PUBLIC_CSP_ENABLED=true

# Allowed origins for CORS (production only)
# NEXT_PUBLIC_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ==========================================
# DEVELOPMENT SETTINGS
# ==========================================
# Enable Next.js telemetry
# NEXT_TELEMETRY_DISABLED=1

# Enable React strict mode
# NEXT_PUBLIC_REACT_STRICT_MODE=true

# ==========================================
# NOTES:
# ==========================================
# 1. NEVER commit .env.local or .env.production to version control
# 2. Use NEXT_PUBLIC_ prefix only for variables needed in the browser
# 3. Server-only secrets (OPENAI_API_KEY, NEXTAUTH_SECRET) should NEVER use NEXT_PUBLIC_
# 4. Generate strong secrets using: openssl rand -base64 32
# 5. Rotate secrets regularly in production
# 6. Use different secrets for development, staging, and production
# 7. Consider using a secrets manager (AWS Secrets Manager, HashiCorp Vault) for production
